<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blueLight</item>
        <item name="colorPrimaryDark">@color/blueDark</item>
        <item name="colorAccent">@color/cremeDark</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="button_layout" parent="">
        <item name="android:textColor">@color/cremeDark</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">15sp</item>
        <item name="android:background">@color/blueDark</item>
    </style>

    <style name="normalStyle">
        <item name="android:textColor">@color/cremeDark</item>
        <item name="android:background">@color/blueDark</item>
    </style>

    <style name="highlightStyle">
        <item name="android:textColor">@color/cremeDark</item>
        <item name="android:background">@color/blueLight</item>
    </style>



</resources>
